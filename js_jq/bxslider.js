$(function(){
    $('.img_btns').bxSlider({
        mode:'vertical',
        controls:false,
        wrapperClass:'category_wrapper',
        auto:true,
        pause:2500,
        pager:true,
        responsive:true,
    });
  });
var deviceWidth = $(window)
if(deviceWidth < 1024){  
}